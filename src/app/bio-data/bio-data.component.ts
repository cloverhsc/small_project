import { Component } from '@angular/core';

// import { HT }   from '../mock-heartbeat';

@Component({
  selector: 'app-bio-data',
  templateUrl: './bio-data.component.html',
  styleUrls: ['./bio-data.component.css']
})
export class BioDataComponent {

    // url = 'http://localhost:8000/api/';

    /*getBioData(targetInfo: string) {
        if ( targetInfo === 'HT') {
            let count = HT.ht.length, i = 0;
            let myVar = setInterval(function(i){
                HT.ht[i++]},
                3000);
            if(i == count-1)
        }
    }*/

}
